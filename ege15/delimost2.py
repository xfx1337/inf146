for a in range(1, 1000): # a = min 1000
    for x in range(1,100000): # x=10a or 100a. если закономерность пропадает, то делаем больше
        f = ((x%34==0) and (x%51!=0)) <= ((x%a!=0)or(x%51==0))    # not ВСЕГДА В СКОБКАХ! или ставить !=
        if f == 0:
            break
    else:
        print(a)